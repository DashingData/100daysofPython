# Program to generate the random numbers

import random
num = random.randint(0 , 2) # randint finds a random number from the given numbers
num2 = random.randrange(30 , 40,  2 ) # in randrange we can become specify as we can choose the differemce between the mumbers also randrange excludes the last number like 40 is not included here 
print(num)
print(num2)